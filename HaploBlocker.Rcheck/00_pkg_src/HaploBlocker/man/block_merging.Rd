% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_merging.R
\name{block_merging}
\alias{block_merging}
\title{Merge blocks}
\usage{
block_merging(blocklist, blockinfo, dataset, dhm, indi, nwindow,
  window_sequence_list, off_lines = 5, min_similarity = 0.99,
  consider_all = TRUE, save_allblock = TRUE, node_min = 0,
  subgroups = NULL, min_per_subgroup = 0, anteil_filter = TRUE,
  helper = NULL, c_dhm = NULL, c_dhm_mode = TRUE,
  intersect_func = intersect, subgroup_exception = 0, run = 1)
}
\arguments{
\item{blocklist}{block-dataset}

\item{blockinfo}{List with all relevant information to each window seperatly}

\item{dataset}{dataset which variant nr. for each window}

\item{dhm}{haploid SNP-dataset}

\item{indi}{number of haplotypes in the dataset}

\item{nwindow}{number of windows in the dataset}

\item{window_sequence_list}{sequence of predefined windows (default: NULL ;per row: start$snp, end$snp, length, length - merging_error, start$bp, end$bp)}

\item{off_lines}{minimum number of haplotypes to looose in the creation of a bigger block (default: 5)}

\item{min_similarity}{minimum rate of the same SNPs to be added to the block (default: 0.99)}

\item{consider_all}{If TRUE always haplotypes which are not in the node to be in a generated block}

\item{save_allblock}{If TRUE keep all haplotypes with all windows according to a block (even under min_similarity)}

\item{node_min}{minimum number of haplotypes per block (default: 5)}

\item{subgroups}{possible subgroups to consider in the block identification process (default: NULL - list(1:indi))}

\item{min_per_subgroup}{minimum number of haplotypes per block per subgroup (default: 0)}

\item{anteil_filter}{Use only relevant Sequences for filter which haplotypes to check in min_similarity}

\item{helper}{Interal information used as a fast abort criterion}

\item{c_dhm}{Bit-wise coded SNP-dataset}

\item{c_dhm_mode}{If TRUE use high speed calculation with C in block_merging (default: TRUE)}

\item{intersect_func}{Used intersect-function (internally relevant for computation time)}

\item{subgroup_exception}{allow for not all subgroups to include a block}

\item{run}{Internal parameter storing the step number}
}
\description{
Function to merge/remove/modify blocks
}
